{
  "description": "Residue purge: sweep list and deprecated smoke stub deleted as files; double-slash unwired-capability markers and audit decision codes rewritten to semantic comments.",
  "scan": ["plugins/**/src/**/*.ts", "plugins/**/src/**/*.tsx", "src/**/*.ts"],
  "forbidFiles": [
    "plugins/errorMessage-sweep.list",
    "scripts/smoke-runtime.mjs"
  ],
  "forbid": [
    { "pattern": "// // unwired-capability", "reason": "double-slash marker format normalized" },
    { "pattern": "leaf-R[0-9]", "reason": "audit decision code replaced by semantic comment or ADR reference" },
    { "pattern": "RD-[0-9][0-9]?", "reason": "audit decision code replaced by semantic comment or ADR reference" }
  ]
}
