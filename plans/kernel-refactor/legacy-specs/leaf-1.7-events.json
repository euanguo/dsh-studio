{
  "description": "WorkspaceEvents cutover: consumer plugins no longer subscribe to the sessions list directly; switching awareness funnels through the kernel events service (which lives outside this scan scope).",
  "scan": [
    "plugins/sidebar/src/**",
    "plugins/pinned-summary/src/**",
    "plugins/plugin-marketplace/src/client/**"
  ],
  "forbid": [
    { "pattern": "list\\.subscribe\\(", "reason": "direct sessions-list subscription replaced by onWorkspaceChanged/onSessionChanged" }
  ]
}
