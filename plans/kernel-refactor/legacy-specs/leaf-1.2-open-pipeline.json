{
  "description": "OpenPipeline cutover: openPath hijack machinery and scattered open signatures must be gone from consumer plugins (the single implementation lives in plugins/workbench, which is outside the scan scope).",
  "scan": [
    "plugins/sidebar/src/**",
    "plugins/plugin-marketplace/src/client/**",
    "plugins/desktop-left-rail/src/**"
  ],
  "forbid": [
    { "pattern": "acquireOpenPathPatch", "reason": "openPath hijack absorbed by pipeline installOfficialOpenHook" },
    { "pattern": "registerOpenPathHandler", "reason": "hijack handler registry replaced by pipeline linkHandler table" },
    { "pattern": "registerLinkInterception", "reason": "link interception owned by pipeline" }
  ]
}
