{
  "description": "SurfaceRegistry unification: the three parallel registration APIs and local descriptor vocabulary are absorbed by the kernel descriptor table.",
  "scan": ["plugins/sidebar/src/**", "plugins/sidebar-desktop/src/**"],
  "forbid": [
    { "pattern": "\\bregisterTab\\b", "reason": "rail tab registration replaced by SurfaceRegistry descriptors" },
    { "pattern": "\\bregisterViewer\\b", "reason": "viewer registration replaced by SurfaceRegistry descriptors" },
    { "pattern": "\\bregisterSurfaceRenderer\\b", "reason": "center renderer registration replaced by SurfaceRegistry descriptors" },
    { "pattern": "\\bCenterSurfaceKind\\b", "reason": "surface kinds come from the shared SurfaceRegistry descriptor vocabulary" },
    { "pattern": "\\bSidebarTabDescriptor\\b|\\bSidebarViewerDescriptor\\b", "reason": "parallel sidebar descriptor types are replaced by one registry descriptor" }
  ]
}
