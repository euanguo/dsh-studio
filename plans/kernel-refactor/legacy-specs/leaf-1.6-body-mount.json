{
  "description": "LayoutService overlay protocol: product plugins have no direct body-level mounts; the shared LayoutDom host is the sole body append writer.",
  "scan": [
    "plugins/*/src/**"
  ],
  "allowFiles": [
    "plugins/shared/layout-dom.ts",
    "plugins/shared/tab-drag-image.ts"
  ],
  "forbid": [
    { "pattern": "document\\.body\\.(append|appendChild)", "reason": "body-level plugin mounts use LayoutDom.mountOverlay" }
  ]
}
