{
  "description": "AppController owns lifecycle; main.ts keeps no module-level lifecycle booleans or runtime handles.",
  "scan": ["src/main.ts"],
  "forbid": [
    { "pattern": "\\btransitioning\\b", "reason": "lifecycle phase lives in controller state machine" },
    { "pattern": "\\bquitting\\b", "reason": "quit intent lives in controller state machine" },
    { "pattern": "\\bpreviewRuntime\\b", "reason": "preview supervisor handle owned by controller" },
    { "pattern": "\\bqueuedPaths\\b", "reason": "path queue owned by controller" }
  ]
}
