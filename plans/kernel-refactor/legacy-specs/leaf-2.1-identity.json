{
  "description": "Desktop identity single-sourced; branding MutationObserver and settings-button DOM scrape removed from src/client.ts (probe relocates to sidebar dsh-dom.ts if still needed).",
  "scan": ["src/main.ts", "src/update-manager.ts", "src/client.ts"],
  "allowFiles": ["src/desktop-identity.ts"],
  "forbid": [
    { "pattern": "euanguo/dsh-studio-app", "reason": "repository identity comes from desktop-identity module" },
    { "pattern": "installHeroBranding", "reason": "upstream copy rewriting observer deleted" },
    { "pattern": "findSettingsButton", "reason": "settings probe relocated out of src/client.ts" }
  ]
}
